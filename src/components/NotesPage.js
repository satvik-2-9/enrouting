import React from 'react';
import PurchasesPage from './PurchasesPage';

const NotesPage = () => {
  return (
    <div>
      <PurchasesPage type='notes' />
    </div>
  );
};

export default NotesPage;
